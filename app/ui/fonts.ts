import { Allura, Average,Montserrat } from 'next/font/google'

export const allura = Montserrat({
    subsets: ['latin'],
    weight: '500'
})

export const average = Average({ subsets: ['latin'], weight: '400' })
